{
  "name": "guest-dashboard",
  "version": "0.0.1",
  "description": "guestlogix guest app team dashboard.",
  "scripts": {
    "preinstall": "cd ./client && npm install && npm install --only=dev",
    "build": "cd ./client && node scripts/build.js"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Guestlogix/glx-pi.git"
  },
  "author": "team guestapp",
  "license": "MIT"
}
